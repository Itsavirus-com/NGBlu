{
  "buildCommand": "rm -rf node_modules package-lock.json && npm install && npm run build",
  "installCommand": "npm install --no-optional --no-audit",
  "framework": "nextjs",
  "functions": {
    "src/app/api/**/*.ts": {
      "maxDuration": 30
    }
  },
  "env": {
    "NODE_ENV": "production"
  }
}
