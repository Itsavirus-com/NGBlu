# Disable optional dependencies to avoid Rollup issues
optional=false
# Use npm ci for faster, reliable builds
engine-strict=true
# Disable package-lock verification in CI
package-lock=false
# Force exact versions
save-exact=true
